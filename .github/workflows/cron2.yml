name: Check Expiration Dates

on:
  schedule:
    - cron: "0 0 * * *"  # Ejecuta la acción cada día a medianoche UTC
  workflow_dispatch:  # Permite ejecución manual

permissions:
  issues: write  # Required to create issues
  contents: read  # Required to read repository data

jobs:
  check-expiration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4  # Descarga el código para leer el JSON

      - name: Check Expiring Customers
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TODAY=$(date -u +%Y-%m-%d)
          echo "Fecha de hoy: $TODAY"

          customers=$(jq -c '.[]' certificates.json)

          for row in $customers; do
              name=$(echo $row | jq -r '.name')
              expirationDate=$(echo $row | jq -r '.expirationDate')

              echo "Cliente: $name - Expiration Date: $expirationDate"

              if [[ -z "$expirationDate" ]]; then
                  echo "⚠️ Expiration date is empty for $name, skipping..."
                  continue
              fi

              # Ensure date command works
              if ! date -d "$expirationDate" +%s > /dev/null 2>&1; then
                  echo "❌ Invalid date format for $expirationDate"
                  continue
              fi

              daysLeft=$(( ($(date -d "$expirationDate" +%s) - $(date -d "$TODAY" +%s)) / 86400 ))

              echo "Cliente: $name - Expira en $daysLeft días"

              if [ "$daysLeft" -lt 30 ]; then
                  echo "Expira pronto, verificando si ya existe un issue..."

                  # Check if `gh issue list` runs correctly
                  if ! gh issue list --json title --state open --search "$name" > issue_list.json; then
                      echo "⚠️ Error running 'gh issue list', skipping issue creation."
                      cat issue_list.json
                      continue
                  fi

                  issue_exists=$(jq -r '.[].title' issue_list.json | grep -c "$name")

                  if [ "$issue_exists" -eq 0 ]; then
                      echo "Creando issue para $name..."
                      gh issue create \
                          --title "⚠️ Renovación pendiente para $name" \
                          --body "La suscripción de **$name** expira en **$daysLeft días** (Fecha de expiración: $expirationDate). ¡Revisar renovación! 🚀" \
                          --repo ${{ github.repository }}
                  else
                      echo "Ya existe un issue para $name, no se creará otro."
                  fi
              fi
          done